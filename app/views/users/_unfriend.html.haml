= form_for(current_user.get_friendship_with(user), html: {method: :delete}, data: {confirm: "Are you sure you want to Unfriend #{user.name}?"}) do |f|
  = f.submit "Unfriend", class: "btn btn-danger"
