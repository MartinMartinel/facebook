%p Friend Request Received
.friend-options
  = form_for(user.friendships.find_by(friended_id: current_user, accepted: false), html: { method: :patch }) do |f|
    = f.submit "Accept", class: "btn btn-primary"
  = form_for(user.friendships.find_by(friended_id: current_user, accepted: false), html: { method: :delete }) do |f|
    = f.submit "Decline", class: "btn btn-default"
